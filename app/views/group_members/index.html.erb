<h1>Listing group_members for <%=@group.display_name %></h1>

<table>
  <tr>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @group_members.each do |group_member| %>
  <tr>
    <td><%= group_member.user.display_name %></td>
    <td><%= link_to 'Destroy', org_group_group_member_path(@org, @group, group_member), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Group member', new_org_group_group_member_path(@org, @group) %>
